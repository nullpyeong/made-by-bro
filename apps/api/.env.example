# apps/api 환경변수 — 실제 값은 .env 에 (커밋 금지). 이 파일은 템플릿.
# 로컬 Docker DB (루트 docker-compose.yml) 기준. 자세히는 ../../SETUP.md
DATABASE_URL="postgresql://madebybro:madebybro@localhost:5432/madebybro?schema=public"
PORT=3000
